# config.toml Reference

<!-- OUTLINE — replace this comment with prose; the option list below is
generated and stays.

- File location table (canonical home for it; Getting Started links here).
- `#:schema` line / tombi.
- Note on `projects.<name>.devcontainer` merge semantics if the generated
  one-liner isn't enough.
- The generated section below comes from the config JSON schema (`just gen`);
  improve it by editing doc comments in crates/cli/src/config.rs.
-->

{{#include generated/config-toml.md}}
