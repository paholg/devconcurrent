# Ports

<!-- OUTLINE — replace with prose.
Source: README-old.md "Ports" tip (L632–655).

- Rule: no static ports in docker-compose.yml — two workspaces will conflict.
- Use `forwardPorts` + `dc fwd` instead.
- The compose `include:` pattern for repos that also serve plain
  `docker compose up` users.
-->
