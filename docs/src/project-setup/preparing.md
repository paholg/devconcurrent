# Preparing a Repository

<!-- OUTLINE — replace with prose. NEW CHAPTER (gap in old README): the old
README mixes user setup with repo-maintainer setup. This section is addressed
to whoever maintains the project's devcontainer, and works as a checklist.

- Audience statement: you maintain a repo and want devconcurrent to work well
  for everyone (users' personal config stays in config.toml; repo config lives
  in devcontainer.json).
- Checklist:
  - Compose-based devcontainer (link: Compatibility).
  - No static ports in compose (link: Ports).
  - External cache volumes + initializeCommand (link: Cache Volumes).
  - The project label on the primary service (for non-dc launches).
  - Optional: proxy (`enable` + per-service `containerPort`), hostname + env
    customizations, mountGit.
- Worked example: a complete minimal .devcontainer/ for a sample project
  (GAP: no full example exists anywhere today; consider an examples/ dir in
  the repo to keep it tested).
-->
