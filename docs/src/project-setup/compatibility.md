# Devcontainer Compatibility

<!-- OUTLINE — replace with prose. NEW CHAPTER (gap): today "compose-only, no
features" is a two-sentence aside; people arriving from VS Code will need this
page.

- Supported: compose-based devcontainers.
- Not supported: image/Dockerfile-based devcontainers, features. Rationale +
  the "put it in your Dockerfile" migration advice; open a ticket if stuck.
- GAP: table of devcontainer.json properties — honored / ignored / partially
  supported. (CONFIGURATION.md's substitution list is a start; a full
  property-support table would preempt many bug reports.)
- Coexistence with VS Code / devcontainer CLI: same container? the project
  label; what `dc` does with a container VS Code created.
-->
