# DNS: Linux Setup

<!-- OUTLINE — replace with prose.
Source: README-old.md "Linux" (L258–322).

- Goal statement: route `.test` to 127.0.0.1:43770.
- systemd-resolved (drop-in file); NixOS (services.resolved); NetworkManager
  (dnsmasq backend, untested — keep the "report back" ask).
- "Your system may differ" escape hatch: what any resolver needs to do.
-->
